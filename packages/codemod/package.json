{
  "name": "@gridstorm/codemod",
  "version": "0.1.0",
  "description": "Codemod CLI to migrate from AG Grid to GridStorm",
  "type": "module",
  "bin": {
    "gridstorm-codemod": "./dist/cli.js"
  },
  "scripts": {
    "build": "tsup",
    "dev": "tsup --watch"
  },
  "files": ["dist"],
  "keywords": ["gridstorm", "ag-grid", "codemod", "migration"],
  "dependencies": {
    "jscodeshift": "^17.0.0",
    "glob": "^11.0.0",
    "chalk": "^5.3.0",
    "commander": "^12.0.0"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "tsup": "^8.5.0",
    "@types/jscodeshift": "^0.12.0"
  }
}
