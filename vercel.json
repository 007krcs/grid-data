{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "installCommand": "pnpm install",
  "buildCommand": "pnpm -r --filter='./packages/*' run build && cd examples/playground && npx vite build",
  "outputDirectory": "examples/playground/dist",
  "framework": "vite"
}
